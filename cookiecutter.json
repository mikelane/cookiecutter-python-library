{
  "project_name": "My Python Library",
  "__project_name": "{{ cookiecutter.project_name.lower().split() | join('-') }}",
  "__package_name": "{{ cookiecutter.project_name.lower().split() | join('_') }}",
  "author_name": "John Doe",
  "author_email": "jdoe@email.com",
  "github_username": "johndoe",
  "short_description": "A basic python library",
  "long_description": "Your description here",
  "license": [
    "MIT",
    "Apache-2.0",
    "BSD-3-Clause",
    "Proprietary"
  ],
  "project_intent": [
    "prototype",
    "project",
    "package"
  ],
  "project_type": [
    "library",
    "cli-app",
    "tui-app",
    "rust-backed",
    "aws-cdk"
  ],
  "python_version_min": [
    "3.11",
    "3.12",
    "3.13"
  ],
  "include_runtime_contracts": "{% if cookiecutter.project_intent in ['project', 'package'] %}y{% else %}n{% endif %}",
  "include_property_testing": "{% if cookiecutter.project_intent in ['project', 'package'] %}y{% else %}n{% endif %}",
  "include_mutation_testing": "{% if cookiecutter.project_intent in ['project', 'package'] %}y{% else %}n{% endif %}",
  "include_dependency_injection": "n",
  "include_bdd_testing": "n",
  "include_benchmarks": "n",
  "include_documentation": "{% if cookiecutter.project_intent == 'package' %}y{% else %}n{% endif %}",
  "use_semantic_release": "{% if cookiecutter.project_intent == 'package' %}y{% else %}n{% endif %}",
  "include_security_scanning": "{% if cookiecutter.project_intent == 'package' %}y{% else %}n{% endif %}",
  "use_codecov": "{% if cookiecutter.project_intent in ['project', 'package'] %}y{% else %}n{% endif %}",
  "publish_to_pypi": "{% if cookiecutter.project_intent in ['project', 'package'] %}y{% else %}n{% endif %}",
  "ci_platforms": "ubuntu,macos,windows",
  "python_versions_test": "3.11,3.12,3.13,3.14"
}
